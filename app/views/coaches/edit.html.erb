<div class='row-fluid'>
	<div class='span-8'>
		<div class= "white">
		<h1>Editing <%= @coach.name %> <%= @coach.age %><%= @coach.national %> </h1>
			<%= render 'form' %>
			<%= link_to 'cancel', coaches_path %>
		</div>
	</div>
</div>